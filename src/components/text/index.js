import styled from 'styled-components'
import { fontFamily, colour, spacing } from '../../constants/style'

export const Title = styled.h1`
  font-family: ${fontFamily.heading};
  color: ${colour.black};
  margin: 0;
  padding: 0;
`
