// import { Link, P } from '../../components/text'

// // takes a tweet object and returns the tweet text with linked users
// export const linkTweet = (tweet) => {
//   // check we have a populated `tweet.entities.user_mentions` value
//   if (!tweet.entities || !tweet.entities.user_mentions || !tweet.entities.user_mentions.length) {
//     return <P>tweet.text</P>
//   }
//   let parts = []
//   tweet.entities.user_mentions.map(mention => {

//   })
// }
